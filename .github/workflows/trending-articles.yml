# DISABLED — Consolidated into daily-news.yml (single pipeline)
# Old: Generated 5 hot-topic articles twice daily
# New: Single run at 6am ET generates 11 articles (3 crypto + 3 IPO + 5 trending)

name: Trending Hot-Topic Articles (DISABLED)

on:
  # Never runs automatically
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: trending-articles
  cancel-in-progress: false

jobs:
  generate-trending:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Consolidated into daily-news.yml"
