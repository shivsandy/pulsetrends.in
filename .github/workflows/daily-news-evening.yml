# DISABLED — Consolidated into daily-news.yml (single pipeline)
# Old: Evening batch for financial articles
# New: Single run at 6am ET generates all 11 articles

name: Daily News — Evening Batch (DISABLED)

on:
  # Never runs automatically
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: daily-news-evening
  cancel-in-progress: false

jobs:
  generate-news:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: echo "This workflow is disabled. Consolidated into daily-news.yml"
